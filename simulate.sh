#!/bin/bash


set -euo pipefail

: ' A script to simulate a group of individuals,
how diverse they are and based on that diversity,
how likely they are to find the ideal solution to a problem.

Written by Philip Miesbauer
'

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null && pwd)"

